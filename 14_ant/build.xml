<project name="Ant" basedir="." default="dist">
    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist" location="dist"/>
    <property name="lib-dir" location="lib"/>
    <property name="test" location="test"/>

    <path id="external.classpath">
        <pathelement location="${lib-dir}/hamcrest-core-1.3.jar" />
        <pathelement location="${lib-dir}/junit-4.12.jar" />
        <pathelement location="${lib-dir}/log4j-api-2.4.1.jar" />
        <pathelement location="${lib-dir}/log4j-core-2.4.1.jar" />
        <pathelement location="${lib-dir}/mockito-all-1.9.5.jar" />
    </path>

    <target name="init" depends="clean" description="initialization">
        <echo message="Initialization has started..."/>
        <mkdir dir="${build}"/>
    </target>

    <target name="compile" depends="init" description="compilation">
        <echo message="Compilation has started..."/>
        <javac srcdir="${basedir}" destdir="${build}" >
            <classpath>
                <path refid="external.classpath" />
            </classpath>
            <include name="${src}\*.java"/>
            <include name="${test}\*.java"/>
        </javac>
    </target>

    <target name="dist" depends="compile" description="Distention">
        <echo message="Distention has started..."/>
        <mkdir dir="${dist}/jar"/>
        <jar destfile="${dist}/jar/Hello.jar" basedir="${build}"/>
    </target>

    <target name="test" description="testing">
        <echo message="Testing has started..."/>
        <mkdir dir="${test}"/>
    </target>

    <target name="clean" description="Delete all generated files">
        <echo message="Cleaning has started..."/>
        <delete dir="${build}"/>
    </target>

</project>