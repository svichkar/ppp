¬ариант 1.

—оздать аннотацию @ToString, котора€ бы позвол€ла аннотировать пол€, которые должны выводитс€ в строковом представлении объекта.
—оздать утилитный класс с методами, позвол€ющими обрабатывать созданную аннотацию и возвращать строковое представление объекта. 

—уществует интерфейс:
public interface PathClassLoader {
    void setPath(String dir);

    String getPath();
}